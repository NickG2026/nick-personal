# Copy to secret.yaml, fill in real values, then:
#   kubectl apply -f k8s/secret.yaml
# Never commit the filled-in secret.yaml.
apiVersion: v1
kind: Secret
metadata:
  name: se-account-manager-secrets
  namespace: se-account-manager
type: Opaque
stringData:
  SLACK_BOT_TOKEN: "xoxb-your-token-here"
  ANTHROPIC_API_KEY: "sk-ant-your-key-here"
